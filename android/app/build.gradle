plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
    // id "com.google.gms.google-services"
}

android {
    namespace = "br.com.w2esolucoes.zona_azul"
    compileSdk = 35 //flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_17
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId = "br.com.w2esolucoes.zona_azul"
        // You can update the following values to match your application needs.
        // For more information, see: https://flutter.dev/to/review-gradle-config.
        minSdk = 23// flutter.minSdkVersion
        targetSdk = 34//flutter.targetSdkVersion
        versionCode = flutter.versionCode
         multiDexEnabled true
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig = signingConfigs.debug
        }
    }

    flavorDimensions "default"

//    productFlavors {
//     saoamarante {
//         dimension "default"
//         resValue "string", "app_name", "cuidaagente saoamarante"
//         applicationId "com.example.zonazurl.saoamarante"
//     }
//     crato {
//         dimension "default"
//         resValue "string", "app_name", "cuidaagente Crato"
//         applicationId "com.example.zonazurl.crato"
//     }
//    }

}



flutter {
    source = "../.."
}
